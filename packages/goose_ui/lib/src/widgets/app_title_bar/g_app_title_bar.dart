import 'package:flutter/material.dart';

//TODO
class GAppTitleBar extends StatelessWidget {
  const GAppTitleBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
