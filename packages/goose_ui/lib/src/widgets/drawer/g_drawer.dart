import 'package:flutter/material.dart';

enum GDrawerState {
  /// 展开的
  open,

  /// 缩起
  shrink,

  /// 关闭
  close,
}

class GDrawer extends StatelessWidget {
  const GDrawer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    //TODO GDrawer
    return Container();
  }
}
